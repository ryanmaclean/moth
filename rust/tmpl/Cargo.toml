[package]
name = "tmpl"
version = "0.0.1"
edition = "2024"
publish = false
description = "{{KEY}} prompt-template substitution and Markdown skill/role loader. Tiny YAML-frontmatter subset, no serde."

[lib]
path = "src/lib.rs"

[dependencies]
wire = { path = "../wire" }
