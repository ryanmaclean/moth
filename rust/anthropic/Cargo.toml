[package]
name = "anthropic"
version = "0.0.1"
edition = "2024"
publish = false
description = "Streaming client for the Anthropic Messages API. libcurl + OpenSSL via FFI, tiny hand-rolled JSON."

[lib]
path = "src/lib.rs"

[dependencies]
curl-sys = "0.4"
wire = { path = "../wire" }
