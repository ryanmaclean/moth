[package]
name = "metrics"
version = "0.0.1"
edition = "2024"
publish = false
description = "DogStatsD-over-UDP emitter. Counters / gauges / histograms / timers. std::net::UdpSocket only — no external deps."

[lib]
path = "src/lib.rs"
